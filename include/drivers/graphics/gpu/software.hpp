namespace Drivers
{
    namespace Graphics
    {
        namespace GPU
        {
            namespace Software
            {
                extern bool inited;
                extern void draw_char(const volatile char &);
                extern void draw_pixel(const volatile char &);
                extern void init();
            };
        }
    }
}