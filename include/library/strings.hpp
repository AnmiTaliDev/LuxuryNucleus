namespace Library
{
    namespace Strings
    {
        extern unsigned long long length_of(const volatile void *of_what);
        extern void clean_asciiz(volatile void *ptr, const volatile unsigned long long &size);
        extern const volatile char *to_string(int);
    }
}