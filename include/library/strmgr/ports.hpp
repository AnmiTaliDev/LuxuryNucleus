namespace Library
{
    namespace strmgr
    {
        extern void outb(volatile unsigned short port, volatile unsigned char value);
        extern unsigned char inb(volatile unsigned short port);
    }
}